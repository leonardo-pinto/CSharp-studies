@using ViewsExample.Models;
@{
    // inside c# code
    // multiple code blocks can be used
    string appTitle = "ASP.NET Core Demo App";
    @*Person person = new() { Name = "Jose", Age = null, Gender = Gender.Male };
    *@
    List<Person> people = new List<Person>()
    {
        new Person() { Name = "John Doe", Age=40, Gender=Gender.Male },
    new Person() { Name = "Jane Doe", Age=30, Gender=Gender.Female },
    };
}
<!DOCTYPE html>
<html>
    <head>
        <title>@appTitle</title>
        <meta charset="utf-8" />
    </head>
    <body>
        <h1>Welcome</h1>
    @foreach (Person person in people)
    {
        <div>
            @person.Name,
            <span>@person.Gender</span>
            @if(person.Age != null)
            {
                <span>@person.Age years old</span>
            }
        </div>
    }
    @* //    @switch(person.Gender)
        {
            case Gender.Male:
                <h1>Welcome, Mr. @person.Name</h1>
                ;
                break;
            case Gender.Female:
                <h1>Welcome, Mss. @person.Name</h1>
                ;
                break;
            case Gender.Unknown:
                <h1>Welcome, @person.Name</h1>
                ;
                break;
        }
    @if (person.Age.HasValue)
    {
        <p>@person.Age years old</p>
    }
    else
    {
        <p>Age is unknown</p>
    }
    *@
   
</body>
</html>